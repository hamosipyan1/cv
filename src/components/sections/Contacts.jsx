import React from 'react';

const Contacts = () => {
    return (
        <section className="min-h-320 bg-green" id="contacts">
            Contacts
        </section>
    );
};

export default Contacts;